---
'@bigcommerce/big-design': minor
'@bigcommerce/docs': minor
---

feat(component): extend `InfoCard`'s `description` prop to accept an object with `text` and an optional `link`, in addition to a plain string
