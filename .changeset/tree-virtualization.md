---
'@bigcommerce/big-design': minor
'@bigcommerce/docs': minor
---

feat(component): add optional virtualization to the `Tree`/`StatefulTree` component via the new `virtualization` prop (`{ maxHeight }`), so large trees (thousands of nodes) render only the rows within the viewport.
