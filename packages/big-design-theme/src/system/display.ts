export interface Display {
  block: string;
  inline: string;
  inlineBlock: string;
  inlineFlex: string;
  flex: string;
}

export const display: Display = {
  block: 'block',
  inline: 'inline',
  inlineBlock: 'inline-block',
  inlineFlex: 'inline-flex',
  flex: 'flex',
};
